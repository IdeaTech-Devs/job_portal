# Database Configuration
DB_HOST=localhost
DB_PORT=5050
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=job_portal

# Server Configuration
PORT=8082
GIN_MODE=debug

# For Railway deployment, use DATABASE_URL instead
# DATABASE_URL=postgresql://username:password@host:port/database 